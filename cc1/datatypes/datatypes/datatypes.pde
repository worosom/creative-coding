//intialisierung einer variable:
//datentyp NAME = wert;
// boolean
boolean yesNo = true;
yesNo = false;
println(yesNo);

// integer, e.g.: 1, 2, 3, 4, …
int zahl = 5;
println(zahl);
zahl = 100000;
println(zahl);

// float, e.g.: 2.5
float kommazahl = 12.7;
println(kommazahl);

// char, "zeichen", e.g.: 'e', 'a', …
char zeichen = 'a';
println(zeichen);

// String, zeichenkette, e.g.: 'katze'
String zeichenkette = "katze";
println(zeichenkette);
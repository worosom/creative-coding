// Logische Operationen

// && - UND
// !  - NICHT
// || - ODER

boolean a = false;
boolean b = true;
println("Variable a:", a);
println("Variable b:", b);
println("a && b:", a && b);
println("!a:", !a);
println("a || b:", a || b);
println("a || !b:", a || !b);